import { IconSchool } from '@tabler/icons-react';

export default function Logo() {
  return (
    <div className='shrink-0 w-8 h-8 bg-primary rounded-lg flex items-center justify-center text-primary-foreground'>
      <IconSchool size={20} />
    </div>
  );
}
